/*const playDie = function (func , time) {
   for (let i = 0; i < time; i++) {
    func();
   }
  };
  
  
  
  const rollDie = function () {
    const roll = Math.floor(Math.random() * 6 + 1);
    console.log(roll);
  };
  
  playDie(rollDie, 3);*/
